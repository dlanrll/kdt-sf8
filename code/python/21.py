def test():
    print("하이")
result = test() # 하이
print(result) # None

# 변수1 = 함수명() 함수의 리턴값을 변수1에 담을게요~
# 변수2 = 클래스명() 클래스(설계도)로부터 만든 산출물(인스턴스)를 변수2에 담을게요~
# 변수3 = 클래스명() 클래스(설계도)로부터 만든 산출물(인스턴스)를 변수3에 담을게요~